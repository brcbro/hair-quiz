{
  "name": "hair-quiz",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Personalized hair care product quiz",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler deploy -c wrangler.jsonc",
    "migrate:firestore": "node scripts/migrate-firestore.js",
    "setup:firebase": "node scripts/setup-firebase.js",
    "firebase:rules": "npx firebase-tools deploy --only firestore:rules"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "firebase": "^12.17.1",
    "nodemailer": "^6.10.0"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "wrangler": "^4.84.1",
    "xlsx": "^0.18.5"
  }
}
